import React from 'react';
import ReactDOM from 'react-dom/client';
import DisplayContact from './AdminPages/DisplayContact';
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<DisplayContact/>);